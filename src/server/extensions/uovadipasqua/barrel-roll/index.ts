import type { Uovadipasqua } from "../../../types/extension";

export const uovadipasqua: Uovadipasqua = {
  id: "barrel-roll",
  triggers: [
    { type: "search-query", pattern: "do a barrel roll" }
  ],
};
